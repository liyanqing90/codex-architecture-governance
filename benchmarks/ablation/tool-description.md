Return only the benchmark observation JSON. Report a finding only when the
fixture directly proves a machine Rule Pack invariant. Use only an allowed Rule
ID; do not use Knowledge or Pattern IDs as Rule IDs. Cite fixture-relative,
contiguous, verbatim evidence with the smallest useful line range. Do not
invent runtime, scale, team, compliance, or production evidence. Put proposed
changes in `observed_recommendations`; use an empty list when no change is
justified.
